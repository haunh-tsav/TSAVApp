export const enum AuthPaths {
  Login = 'login',
  Register = 'register',
}

export const enum Paths {
  Explore = 'explore',
  Home = 'home',
  Profile = 'profile',
  Search = 'search',
}

export const enum RootPaths {
  App = 'app',
  Auth = 'auth',
}
